import React from "react";
import { render } from "@testing-library/react";
import "@testing-library/jest-dom/extend-expect";
import VerRutas from "./front-end/components/ruta/VerRutas/VerRutas";
/*
test("comprueba título y cabecera del componente VerRutas", () => {
  const expectedValues = {
    title: /Mis rutas/i,
    header: /En este apartado puedes echar un vistazo a tus rutas, visualizarlas en un mapa, ver sus detalles o bien eliminarlas./i
  };

  const { getByText } = render(<VerRutas />);
  const title = getByText(expectedValues.title);
  const header = getByText(expectedValues.header);

  expect(title).toBeInTheDocument();
  expect(header).toBeInTheDocument();
});
*/
