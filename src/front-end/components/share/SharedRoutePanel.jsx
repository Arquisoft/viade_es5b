import React, { Component } from 'react';

/**
 * Componente que modela un panel con la información
 * de una ruta compartida por un usuario.
 */
class SharedRoutePanel extends Component {
    state = {  }
    render() { 
        return (  );
    }
}
 
export default SharedRoutePanel;